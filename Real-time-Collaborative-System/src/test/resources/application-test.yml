spring:
  datasource:
    url: jdbc:h2:mem:foundation;DB_CLOSE_DELAY=-1
    username: sa
    password: ''
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
collabnote:
  web:
    cors:
      allowed-origins:
        - http://localhost:5173
