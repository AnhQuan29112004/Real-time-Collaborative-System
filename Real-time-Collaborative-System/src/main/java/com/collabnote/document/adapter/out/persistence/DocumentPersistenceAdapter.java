package com.collabnote.document.adapter.out.persistence;

import com.collabnote.document.application.port.out.DocumentRepositoryPort;

/** Outbound adapter/persistence contract dự kiến; chưa có integration hoặc JPA mapping. */
public class DocumentPersistenceAdapter implements DocumentRepositoryPort {
}
